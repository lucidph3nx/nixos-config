# Session lifecycle invariants

<!-- doclint-ignore: manifest.json, raw/session.jsonl, dist/core/session-manager.js, modules/programs/prism/tmux.nix -->
<!-- doclint-ignore: modules/system/impermanence.nix, modules/programs/prism/prism-tui.nix -->
<!--
  `manifest.json` and `raw/session.jsonl` are runtime-created artifacts
  inside archive directories under `~/.local/share/prism/archive/...`;
  they are described in the archive layout section but never exist in
  the source tree. `dist/core/session-manager.js` is a file in the
  external pi coding-agent package (cited for the ENV_AGENT_DIR
  honouring lookup), not this repo.

  `modules/programs/prism/tmux.nix` is different from those three: it is
  a real, tracked file in this repo, and the reference is correct. It is
  unresolvable only during the build. doclint resolves a file path under
  the prism Go source root or the repo root, and the nix sandbox copies
  the prism Go subtree alone — so the path resolves in a repo checkout
  (and in `go test ./...`) and cannot resolve in
  `nix-build-prism-checked`. The reference stays because it names the
  exact mechanism the release ordering above depends on: that file
  registers the global tmux `session-closed` hook. Same class as the
  out-of-subtree entries in
  `docs/diagnoses/review-agent-no-verdict-1993.md`.

  `modules/system/impermanence.nix` and
  `modules/programs/prism/prism-tui.nix` are the same class as
  `modules/programs/prism/tmux.nix`: real tracked files, correct
  references, out of the prism Go subtree that the nix sandbox copies
  in. They name the two entries that keep the Go caches across a boot
  wipe (issue #2731), so the bwrap warm-cache invariant depends on
  them by name.
-->

This document is a falsifiability surface for the user to mark against intent.
Each statement describes a user-observable behaviour of prism session lifecycle
as it currently exists. It is **not** a tutorial, design rationale, or rollout
plan — read it once, mark any line that does not match intent, and the marked
lines feed follow-up tickets. Statements are deliberately phrased so they can
be verified by running prism and observing, without reading code.

Unless a statement names a specific session kind or isolation mode, it applies
to all of them. The valid isolation modes referenced below are `bwrap`,
`sandbox-exec`, and `host`; there is no `podman` mode in the current code.

## Spawn

- `prism spawn` creates a git worktree at `<bare-root>/<branch>/`, a tmux session named `<repo>@<branch>`, an `agent_status` row, a `sessions` row keyed by `instance_id`, a per-session run directory at `$XDG_STATE_HOME/prism/run/<12-hex-of-sha256(session)>/`, and allocates one harness port from the configured range.
- The `instance_id` for a new session is minted host-side as a UUID before the tmux session and sidecar start, and is written to both `agent_status.instance_id` and `sessions.instance_id`.
- The initial `agent_status.state` written at spawn time is `idle`; it transitions to `active` only after the harness handshake or the first `turn_start`.
- When a non-empty `--prompt` is supplied, the prompt body is written to the per-session run directory and delivered to the agent by file path — via `PRISM_INITIAL_PROMPT_FILE=<path>` on the agent tmux pane for every isolation mode (`host`, `bwrap`, `sandbox-exec`), and additionally via `--prompt "$(cat <path>)"` inside the agent command for the 3-window host-mode spawn layout. The prompt body is never inlined into the tmux launch argv.
- `prism spawn` rejects an empty `--prompt` for agent-bearing layouts, and rejects any spawn whose computed host-side launch command would exceed the safe size bound — in both cases before any tmux state or DB side effects are created.
- Two concurrent `prism spawn` calls for the same session name in the same prism process serialise through an in-process lock; the second caller sees the live row and exits non-zero with `"is already alive ... concurrent spawn rejected"`.
- `prism spawn --branch <name>` without `--reuse` fails when an active session already exists on that branch; with `--reuse` it returns the existing session's name, port, and agent and exits 0. For `--branch main` specifically, reuse semantics are the default — no `--reuse` flag required (#2352). Passing `--reuse` alongside `--branch main` is accepted and behaves identically to omitting it.
- `prism spawn --wait` blocks until the spawned session's first turn reaches `finished`, `error`, `interrupted`, or `deleted`, with a default timeout of `10m`.
- `prism spawn` enforces a per-isolator concurrency cap for `bwrap` and `sandbox-exec` (compiled-in default `50` each; configurable via `bwrap_concurrency_cap` / `sandbox_exec_concurrency_cap` in `~/.config/prism/config.json`) and refuses to start a new session over the cap unless `--ignore-concurrency-cap` is passed; `host` mode is uncapped.
- `prism spawn --branch main` in a bare+worktree repo (where `<repo>/main/` already exists as a worktree) starts a detached coordinator session on the existing worktree rather than failing at `git.CreateWorktree` (#2352). When an active healthy `<repo>@main` session already exists, the command prints its details and exits 0 (broken sessions in `error`/`deleted` state surface the structured `prism cleanup` hint instead); when `--prompt` is supplied, it is delivered to the running coordinator (subject to the standard `waiting`-state guard, which refuses with the same message shape as `prism prompt`). When no main worktree exists at `<repo>/main/` (e.g. the repo default branch is not `main`), the command falls through to `git.CreateWorktree` unchanged. The single-coordinator-per-repo invariant is preserved by the active-session dedupe check; no branch other than `main` receives this default-reuse treatment.
- The default isolation mode for a new session is the machine-level `default_isolation_mode` from `~/.config/prism/config.json` (default `host`), optionally overridden per-path by `project_isolation_overrides`.

## State transitions

- The canonical agent states are `idle`, `active`, `waiting`, `compacting`, `reviewing`, `escalated`, `finished`, `interrupted`, `error`, and `deleted`.
- A session enters `active` from `idle` on the first `turn_start` frame, on `session.created` from the SSE stream, or on the first `server.heartbeat` when neither has fired yet.
- A session enters `waiting` on `permission.asked` or `question.asked`; it returns to `active` on `permission.replied` or on `question.replied`/`question.rejected`.
- A session enters `compacting` on a `session.updated` with `time.compacting` and returns to `active` on `session.compacted`; the compacting transition does not notify the coordinator.
- A session enters `reviewing` when the worker calls `prism review` (written before any review agents spawn). It leaves `reviewing` for `active` when the review-complete prompt is delivered with `source=review-complete`, and for `finished` when the worker produces a clean turn after that delivery.
- A session enters `escalated` when the worker calls `prism escalate`. While the same-turn guard `escalatedInFlight` is armed (set by the host-API `/escalate` handler when the host-side child exits 0, for the sidecar's own session), `turn_start` frames and non-terminal `state_change` frames from the remainder of the escalating turn are suppressed and do **not** clobber the `escalated` state — pi emits `turn_start` per agent-loop iteration, so without the guard the iteration following the escalate bash call would silently overwrite it (issue #2255). The guard clears when the finished debounce fires (the escalating turn has fully ended), when a prompt is delivered to the session via `/prompt` or the reconnect replay buffer (incoming guidance), or on a terminal transition. After the guard clears, any incoming `turn_start` from any source (a delivered prompt, a human typing into tmux) transitions the session back to `active`. Host-isolation-mode sessions never arm the guard (no `/escalate` proxy hop) and retain the unguarded behaviour.
- Entry into `escalated` is valid from `active`, `idle`, and `error` (issue #2359). The `idle` and `error` source states cover the dead-pipe class: a session whose harness pipe never delivered a turn event sits at `idle` (no turn events flowing) or `error` (startup-handshake timeout stamped it) while pi is alive; `prism escalate` from those states must still enter `escalated` so the finish-notification suppression and sessions-list visibility contract applies uniformly. The `active`→`escalated` transition and the same-turn guard semantics are unchanged.
- While a session is in `escalated`, the durable pending-replay buffer (see Notifications) is the mechanism that carries a coordinator's reply back to the worker across sidecar restart — a `prism prompt` delivery buffered before the worker's sidecar cycles is flushed on the next handshake with `replay: true` and survives the restart.
- A session enters `finished` after a 2-second idle debounce that begins on `session.idle` (SSE path) or `state_change{finished}` (PI path), or immediately after a `session_shutdown` frame.
- A session enters `error` on `session.status{retry}`, on `session.error` with any name other than `MessageAbortedError`, or when the inactivity watchdog fires after `cfg.ActivityTimeout` of no inbound frames.
- A session enters `interrupted` on `session.error` with name `MessageAbortedError`, on a manual permission denial when the next idle debounce fires, or on a pane-died tmux hook with a non-zero exit (even from a previously-`finished` state).
- A session enters `deleted` on `session.deleted`; `deleted` is terminal with no outgoing transitions.
- A session can resume from `finished`, `interrupted`, or `error` back to `active` via `session.updated` (clearing `ended_at`), but a `session.updated` arriving within `ErrorResumeDebounce` of a `session.error` does not transition state — it only refreshes metadata.
- A session in `escalated` does **not** emit the `Agent <name> has finished its current task` notification when its turn ends; the `session.escalated` bus event has already informed the coordinator. This suppression also covers the `session_shutdown` frame: a terminal exit while escalated still writes `finished` (a valid terminal transition) but the finish notification stays suppressed — the pre-write state is checked before the `finished` upsert so the guard cannot be defeated by its own write ordering (issue #2255).
- A session in `reviewing` does **not** emit the `has finished` notification on `session.idle` / `state_change{finished}`, and suppresses the `turn_start`-driven `active` write so that incidental assistant turns do not clobber the `reviewing` state.

## `prism prompt` and the waiting guard

- `prism prompt <session> --prompt <text>` refuses to deliver when the target session is in `waiting` and exits non-zero with a message instructing the caller to switch to the session and respond directly.
- `prism prompt` against a session whose `ended_at` is non-NULL returns `"session ... has ended — escalate to user to restart if needed"`; against an unknown session it enumerates up to 10 active session names in the error.
- `prism prompt` delivery is idempotent on `delivery_id`: a repeat with the same UUID is dropped before reaching the harness pipe and the response carries `"replayed": true`.
- `prism prompt --deliver-as` accepts `steer`, `followUp`, or `nextTurn`; the CLI validates the value before any network call.
- `prism prompt` surfaces the receiving sidecar's response envelope to the caller: when the target's PI extension is disconnected, the sidecar responds `{"buffered": true}` (the delivery has been accepted and buffered for flush on the next handshake) and the CLI prints a human-readable line naming the buffered outcome (`prompt accepted for <session> but PI extension is disconnected — buffered for replay on next handshake (delivery_id=<uuid>)`). Exit code stays `0` — the delivery is contractually promised and durably persisted (see Notifications). The `--json` flag emits a single-line JSON object on stdout with the fields `delivered_to`, `delivery_id`, `buffered`, `replayed`, and `transport` (`socket-pipe` for pi harnesses, `http` for HTTP harnesses, `host-api` for the sandboxed proxy path). A dead-socket or otherwise unreachable target sidecar exits non-zero with an actionable error naming the socket path — never silent success (issue #2359).
- When the calling process's `PRISM_HOST_API` is set (every sandboxed agent), `prism prompt` routes through the sandbox sidecar's `/prompt` endpoint, which shells out to host-side `prism prompt --json` and forwards the child's `buffered`/`replayed` envelope back through the response — the sandboxed caller sees the same surfaces (human line + `--json`) as a direct-host invocation.

## Persistence & cleanup

- `prism cleanup --yes --session <name>` for a feature-branch session takes the **hard-cleanup** path: removes the worktree at `<bare-root>/<branch>/`, force-deletes the local branch, kills the tmux session, redirects any attached clients to `scratchpad`, and after archiving removes the cleaned session's own pi transcript JSONL from the sessions root (gated per #2336). Passing `--keep-worktree` downgrades this to the soft-close path — see the coordinator/non-`@` bullets below for the preserved surfaces (#2371).
- `prism cleanup --yes --session <name>` for a coordinator session (one whose `root_agent_name = 'coordinator'`, or whose branch matches the default branch when `root_agent_name` is NULL) takes the **soft-close** path: kills the tmux session and marks the DB row ended, but **does not** remove the worktree, delete the branch, or delete the cleaned session's own pi transcript from the sessions root — pi's interactive `/resume` keeps working on the retained worktree (#2371).
- `prism cleanup --yes --session <name>` for a non-worktree session (no `@` in the name) takes the **soft-close** path: kills the tmux session and marks the DB row ended; no git operations run and the cleaned session's own pi transcript is preserved in the sessions root (#2371).
- `prism cleanup --yes` always force-deletes the feature branch and does not check `git branch --merged` against `main`, because squash-merges produce a different SHA on `main` than the branch tip.
- `prism cleanup` invoked inside a sandbox (where `PRISM_HOST_API` is set) proxies the cleanup request to the host sidecar; per-resource progress lines are forwarded verbatim.
- `prism cleanup` releases the session's allocated `harness_port` (sets `agent_status.harness_port = NULL`), clears `agent_status.harness_session_id` (cascading to `<session>~review-%` children via SQL LIKE), sets `agent_status.ended_at`, sets `sessions.ended_at` and `sessions.end_state = "finished"`, writes a `spawn_outcome` row, purges queued bus messages for the session, and removes the per-session host-API socket, the `agent-run.log`, and their per-session run directory under `$XDG_STATE_HOME/prism/run/<sessionDirHash>/`.
- The `harness_port` release and `ended_at` stamp run regardless of whether the worktree path was found and regardless of whether the tmux session was still alive at cleanup time; they also run for sessions without a worktree (review subsessions named `<parent>~review-<N>-<agent>`). The `harness_session_id` clear is sequenced separately: it runs **after** the archive step (on hard cleanup the resume-linkage sever deletes the same transcript JSONL the archive copies). On **hard-cleanup** paths it is skipped entirely in TWO classes — (a) when the archive step returns an error (#2219), and (b) when the archive step ran but the harness adapter reported no transcript was actually copied (the manifest-only cases: adapter's `Archive` saw `IsNotExist(srcPath)`, or `SourcePath` returned a sentinel path for skip paths 1–6 in `runSessionArchive`, #2336). In both classes the on-disk transcript and the resume pointer are left intact so a later cleanup re-run can still archive-then-sever. On **soft-close** paths (`prism close` soft decision, `prism cleanup --keep-worktree`, coordinator/non-`@` sessions) the sever is DB-only — the transcript is never deleted — and runs unconditionally, even when the archive step failed, so a re-spawn on the same session name always starts a fresh conversation (#2371, #2035).
- Re-running `prism cleanup --yes --session <name>` on an already-ended session is idempotent: the `harness_port` and `ended_at` updates are no-op successes, the `ended_at` timestamp is not re-stamped (the `WHERE ended_at IS NULL` guard suppresses the UPDATE), and the command exits 0. When the first run already wrote the archive, the re-run's archive step returns `ErrAlreadyExists`; the `harness_session_id_cleared` envelope value then depends on close mode. On a **hard-cleanup** re-run it is reported as skipped (`"skipped: archive failed: …"`) rather than `true` — the pointer was already cleared and the transcript already archived-and-removed by the first run, so nothing is lost or re-deleted (#2219). On a **soft-close** re-run (coordinator, non-`@`, `--keep-worktree`, or open-PR worker via `prism close`) the DB-only sever runs unconditionally and the envelope reports `true` (idempotent no-op DB clear); the transcript was never removed on the first run and is still preserved (#2371).
- `prism cleanup` does **not** stamp `ended_at`, release a port, or clear a `harness_session_id` for an `agent_status` row that does not exist — there is no implicit row creation. An unknown session name is rejected up-front with an enumerated error by `cleanupCmd.RunE`'s pre-flight DB validation.
- `prism cleanup` for `sandbox-exec` sessions also removes the per-session work directory at `$XDG_STATE_HOME/prism/sessions/<instance_id>/` (generated ssh-config/gitconfig/allowed_signers, kube-cache, glab-cli, chromium Library skeleton — #2213/#2247/#2668). The removal is a whole-tree `RemoveAll`, so any legacy per-instance staging `HOME` remnant at `<sessionDir>/home/` left behind by a pre-Step-5-of-#2132 session is swept too (the staging-HOME mechanism itself was deleted in Step 5 — #2250). `host` and `bwrap` modes have no work-dir equivalent.
- `prism cleanup` for a worker stops and removes every child review-agent container (30-second per-child budget) and then kills every `<worker>~review-N-<role>` session and releases each child's port and DB row.
- `prism cleanup` does **not** clean up investigator child sessions of the cleaned-up session — investigators must be cleaned up by their invoker with a separate `prism cleanup --yes --session <inv-session>` call.
- A session whose tmux process died without prism cleanup (a "zombie") leaves the `agent_status` row, the allocated port, and the worktree behind; `prism cleanup --yes --session <name>` is the supported recovery and removes them.
- The stale-zombie classification used by `prism switch` (and `startupGuardKillOld`) treats a session as live when its `agent_status.last_seen` is under 60 seconds old **or** its sidecar responds on one of its Unix sockets (`hostapi.sock` / `pipe.sock`, probed via `session.SidecarAlive`). Paused-by-design states (`escalated`, `reviewing`, `waiting`) freeze `last_seen` for arbitrarily long while the sidecar is healthy; without the socket probe the heuristic would SIGTERM the healthy sidecar and sever the session's coordination channels (issue #2255). Genuinely dead sidecars — absent socket files or an ECONNREFUSED tombstone — still classify as zombies and take the kill-and-recreate path.
- The `agent_status` and `sessions` DB rows themselves are **not** deleted by cleanup — only their `ended_at` / `end_state` fields are updated; the rows remain queryable as audit/history.
- The OAuth token store, the user's profile config (`~/.config/prism/profiles.json`), and the `feedback.jsonl` log all live outside the per-session directory and survive cleanup unchanged.
- Branch deletion is best-effort on the hard-cleanup path — a failure deleting the branch does not abort cleanup; the worktree-removal and sidecar/tmux teardown still complete. Soft closes never attempt to delete the branch (the branch is deliberately preserved along with the worktree — #2371).
- `prism cleanup --yes --json` emits a single JSON object on stdout with the fields `session`, `worktree_removed`, `branch_deleted`, `session_killed`, `ended_at_stamped`, `harness_port_released`, and `harness_session_id_cleared`; per-step textual progress is suppressed and warnings still go to stderr.
- Two further envelope fields report the orphan-resource sweep: `containers_swept` and `volumes_swept`. Both are gated on one `agent_status.containers_enabled` read: a session with `containers_enabled = 0` issues no podman command at all and its envelope omits both keys, which is what makes "cleanup touched no container runtime" observable from the outside. When a sweep did run, its key is present even when the count is `0`. A count reports only what podman confirmed removed — a failed podman invocation contributes `0` rather than a guess, so the counts are a floor, not a total. Images the session pulled are **not** swept and have no envelope field; `docs/podman-proxy.md` §8.3 records the two conditions that work needs first.
- The **container** sweep runs on every teardown path. The **volume** sweep runs on the hard-cleanup paths only, so `volumes_swept` is absent from a soft-close envelope even when `containers_swept` is present. A soft close preserves the worktree, the branch, and the transcript so the session can be reopened, and a volume holds data where a container does not — the volume sweep matches on a plain `prism-<session>-` name prefix precisely so it reaches user-named data volumes, so running it on a preservative path would destroy state the operator expected to survive. The volumes are swept later, when the session reaches hard cleanup.
- The three lifecycle-bookkeeping fields (`ended_at_stamped`, `harness_port_released`, `harness_session_id_cleared`) in the `--json` envelope are heterogeneous: each is `true` on success (including idempotent no-ops where the row is already in the cleaned-up state), a string describing the failure on error, or `null` when the DB block was never reached (e.g. the function returned early via the `PRISM_HOST_API` proxy short-circuit). `harness_session_id_cleared` has two additional skip-report value classes: `"skipped: archive failed: <err>"` when the sever did not run because the archive step failed (#2219), and `"skipped: transcript missing"` when the sever did not run because the archive step ran but the harness adapter reported no transcript was actually copied — the manifest-only case (#2336). Both are skip reports, not sever failures, and both apply to **hard cleanups only**: on a soft close the sever is DB-only and unconditional, so `harness_session_id_cleared` reports `true` even when the archive step failed, and a soft re-close reports `true` (idempotent no-op DB clear) rather than the archive-collision skip string (#2371). When `openDB()` fails inside the cleanup function itself, all three fields carry the same `"db open failed: <err>"` description so the operator can distinguish "bookkeeping ran successfully" from "bookkeeping was skipped because the DB was unavailable".

## Archive

- `prism cleanup` archives the harness session storage into `~/.local/share/prism/archive/<repo>/<startedAtISO>_<instanceID>/` containing `manifest.json`, (when the harness wrote conversation data) `session.jsonl`, and (when present) `agent-run.log`.
- The archive copy is performed under a `.tmp-<instanceID>/` directory and renamed atomically on success; a partial archive is removed on any error. Archive directories are mode `0700`, files mode `0600`.
- When the final archive directory already exists, the archive step returns `archive.ErrAlreadyExists`; cleanup logs a warning and continues without re-archiving — worktree, branch, tmux, and DB teardown still complete. On **hard cleanup** the resume-linkage sever is skipped in this case like any other archive error (#2219); on a re-run of hard cleanup this is a no-op because the first run already severed. On a **soft close** the DB-only sever runs regardless of the collision (#2371) — a no-op re-clear on a re-close, with the envelope reporting `true` — and the transcript is untouched either way.
- For `pi` harness sessions, `session.jsonl` is the full PI JSONL transcript copied directly from pi's on-disk format (which is already pi-mono v3 shaped — no normalisation step is required); sessions where PI never wrote any data archive only `manifest.json`, with no `session.jsonl`. The manifest-only shape is also produced when the archive step reaches `archive.Run` but the pi adapter cannot locate a matching transcript on disk (e.g. `harness_session_id` drifted mid-session because pi rotated its internal session id) — in that case `sessions.archive_path` is still populated for the incarnation, but the hard-cleanup resume-linkage sever is skipped (adapter reports `copied == false`, #2336) so the on-disk pi sessions dir is not modified (soft closes never modify the pi sessions dir and clear the DB pointer regardless — #2371). Pre-fix archives have a `raw/session.jsonl` + `session.jsonl` pair under the old two-stage layout; those archives are left as-is on disk — `prism archive` only prints the recorded path.
- The PI JSONL transcript copied into the archive is sourced from the host PI sessions root for **all** isolation modes (`<piSessionsRoot>/<encoded-cwd>/` — `$PI_CODING_AGENT_DIR/sessions/` when the env var is set, else `~/.pi/agent/sessions/` — which is not owned by prism). `sandbox-exec` is no exception: pi writes to the host root because the dispatcher injects `PI_CODING_AGENT_DIR` into the sandbox env (#2210); the per-session work directory never contains transcripts (nor did the per-instance staging `HOME` it superseded — deleted in Step 5 of #2132). The sessions root itself survives cleanup; the cleaned session's own `*_<harness_session_id>.jsonl` is removed **on hard cleanup only**, by the resume-linkage sever (#2035), only **after** the archive step has copied it into the archive AND the harness adapter has reported the transcript was actually copied — when the archive step fails (#2219) or when it produces a manifest-only archive (adapter reports `copied == false`, #2336), the hard sever is skipped and the transcript is left in place — while sibling transcripts in the same `<encoded-cwd>` directory are untouched. Soft closes preserve the transcript entirely so pi's interactive `/resume` keeps working on the retained worktree; only the DB pointer is cleared (#2371).
- `prism cleanup` writes the archive directory path to `sessions.archive_path` for the incarnation's row.
- `prism archive <instance-id|session-name>` prints the recorded `archive_path` and exits 0 when present; it exits non-zero when the row is unknown or `archive_path IS NULL`. `prism archive <session-name> --all --json` emits a JSON array of every archived incarnation for the name, newest first.

## Resume

- For PI sessions, `prism agent-run` appends `--session <HarnessSessionID>` to the `pi` invocation when the on-disk session JSONL exists under the mode-aware sessions root; pi then reopens the prior conversation.
- When the on-disk PI session JSONL for a known `HarnessSessionID` is missing, the agent starts a fresh conversation and a tagged warning is appended to the per-session `agent-run.log` — the spawn does not fail.
- The PI sessions root is the **same for every isolation mode**: `$PI_CODING_AGENT_DIR/sessions/` when the env var is set on the prism CLI's environment, else `~/.pi/agent/sessions/`. For `bwrap`, the directory is overlay-mounted into the sandbox at `$PI_CODING_AGENT_DIR/sessions/`; for `sandbox-exec`, the dispatcher injects `PI_CODING_AGENT_DIR=<host ~/.pi/agent>` into the sandbox env and the sandbox shares the host filesystem, so pi writes to the host root there too (#2210 — pre-fix the resolvers wrongly pointed `sandbox-exec` at `<staging-home>/.pi/agent/sessions/`, so resume never found the transcript). The env-var lookup mirrors pi's own `ENV_AGENT_DIR` honouring (pi 0.79 `dist/core/session-manager.js`). The `<encoded-cwd>` directory is shared across any prism session whose worktree matches the cwd, in every mode.
- A `session.updated` frame for a session whose current state is `finished` or `interrupted` clears `ended_at` and transitions the session back to `active`. Re-creating the tmux session for a previously-terminal session via `tmux-session-start` resets `agent_status.state` to `idle`.
- Once a **hard cleanup** has run, the worktree, branch, tmux session, port, and the cleaned session's own pi transcript from the sessions root are gone and cannot be recovered; only the archive at `~/.local/share/prism/archive/...` remains. After a **soft close** the tmux session and port are gone, but the worktree, branch, and pi transcript are all preserved — only the DB resume pointer is cleared, so a follow-up `prism spawn` on the same branch starts a fresh conversation (#2035) while pi's interactive `/resume` still lists the prior transcript for the operator to pick up manually (#2371).

## Session kinds

- A **root session** is the top-level session of a project: `<repo>@main`, or a bare name with no `@` and no `~` for a project that has no worktree (e.g. `obsidian`). The predicate is `authz.IsRootSession` in `internal/authz/session.go` (issue #2658). It is read at exactly two places — the cross-repo arm of the host-API `/prompt` gate, and the cross-repo arm of `db.AllActiveStatusForRepoAndOtherRootSessions`, which backs `prism sessions list`. It grants nothing else: the merge queue, `prism investigate`, `prism review`, the profile override, the permission audit, and the `prism checkin` tiers all keep reading `IsCoordinatorSession`. Every coordinator is a root session; the reverse does not hold.
- A name that holds `~` is **never** a root session and **never** a coordinator, whatever its `root_agent_name` column says. The guard is `sessionname.IsDescendant` and it is applied before any DB read, so a wrong column value cannot promote a review agent or an investigator. The same guard is in `authz.IsCoordinatorSession`, `session.IsCoordinatorSession`, and `db.retroIsCoordinator`.
- A **meta-session** (`scratchpad`, `prism-dashboard`, per `sessionname.MetaNames`) is never written to `agent_status`, is never listed, and is never classified as a coordinator or a root session.
- A name that holds `~` in the **repo** part but not the branch part — `weird~repo@main` — is a coordinator, not a descendant. `sessionname.IsDescendant` searches the branch part only (everything after the first `@`, or the whole name when there is no `@`), and the `sessions list` SQL mirrors it. One ambiguity is inherent and unresolvable from the name: a BARE repo directory whose own name holds `~` reads as a descendant. That resolves toward refusing privilege.
- `agent_status.repo` has several writers, and `db.UpsertStatus` sets `repo = excluded.repo` with no COALESCE, so the LAST writer wins outright. `prism sidecar` is the continuous last writer: `internal/sidecar/state.go` re-writes the column from `sidecar.Config.Repo` on every state transition. A repo value seeded at spawn time therefore does not survive unless the sidecar derives the same value. Every writer in `cmd/` must derive it with `sessionname.Repo`; `TestNoPrivateRepoGrammarInAWriter` (`cmd/repo_attribution_test.go`) fails when a writer grows a private copy of the grammar (#2658).
- **Known defect, not fixed by #2658.** `prism sessions list` run FROM a bare-name session fails on the direct CLI route. `cmd/list_sessions.go` derives the current repo from the filesystem path (`repoFromWorktreePath(cwd)`), which returns `""` for a directory with no `.bare` marker, and the command then refuses with "is not inside a prism worktree; use --all". The host-API route does not fail, because it derives the repo from the session name through `repoFromSession`, which reads the DB first. A `host`-mode session has no socket, so it takes the failing route. This is the inbound mirror of the outbound defect #2658 fixes, it predates #2658, and it is a two-route divergence of the class #1830 exists to forbid.
- **Known defect, not fixed by #2658.** `db.ParentSessionFor` keeps the `@`-only rule in its name-heuristic fallback, so it returns `""` for a descendant of a bare-name parent. The `group_id` path resolves such a row first, so the fallback only applies to a row with a NULL `group_id`.
- **Known defect, not fixed by #2658.** `cmd/event.go` writes `repo = ""` for a session with no prism worktree (a deliberate, tested contract — see `TestEventTmuxSessionStart_NonWorktreeSession`), while `prism sidecar` writes the name-derived repo for the same row. The two race, and a row that ends with an empty repo is unreachable: `authz.RepoFromSession` skips an empty column, fails the name parse, and answers "not found", so `prism prompt <bare-name>` returns 404. In practice the sidecar wins, which is why the issue's evidence row shows a populated repo. Resolving the disagreement changes a tested contract and reaches `prism restore`, so it needs its own issue.
- The repo of a session name is the prefix before the first `@`; when the name holds no `@`, it is the prefix before the first `~` (`sessionname.Repo`). `@` is tested first so a repo directory whose name holds `~` is not truncated. So `obsidian` and `obsidian~investigate-v2` both belong to repo `obsidian`. `agent_status.repo` is authoritative for a live session; `authz.RepoFromSession` reads it first and falls back to the name rule for `@`-bearing names only. A name with no `@` and no row is an unknown session and yields a 404, which is what stops the bare-name arm of `IsRootSession` from admitting a typo.
- A **coordinator** is identified by `root_agent_name = 'coordinator'`, with a fallback name heuristic of `<repo>@main` for pre-migration rows. Coordinators receive `has finished`/`has errored` notifications from workers in the same repo and own the merge-queue watcher for their session lifetime.
- A coordinator does **not** receive a `has finished` notification for its own session ending — the self-notification guard suppresses it. A coordinator cannot run `prism review` directly; the review verb is for worker / spawned sessions.
- Cleaning up a coordinator session takes the soft-close path: kills the tmux session, ends the DB row, and writes the archive, but **keeps the worktree, branch, and pi transcript intact** (#2371) so a reopened coordinator can `/resume` the prior conversation.
- A session created by `prism pr <number>` or `prism spawn --pr <number>` always targets a pre-existing PR (Case 1 in the coordinator agent's instructions) and is injected with read-only guidance via `withPRReadOnlyGuidance`; a plain `prism spawn` session (branch or default) receives none and keeps the ordinary commit-and-push flow (Case 2). See `cmd/pr.go` and `cmd/spawn.go` for the injection points.
- A **worker** is any non-coordinator session created by `prism spawn` (typically with `root_agent_name = 'worker'`). Workers may run `prism review` and `prism escalate`, and may check in on the review agents of their own session (see Checkin scope below). The host-API role gate refuses `prism investigate`, `prism merge`, `prism merges list`, and `prism merges cancel`: `requireCoordinator` in `internal/sidecar/host_api.go` answers HTTP 403. A worker outside a sandbox has no socket to route through. Each of those verbs therefore carries a second coordinator guard on its direct CLI path: `requireInvestigateCoordinator` in `cmd/investigate.go`, the coordinator-only branch in `cmd/merge.go`, and `requireMergesCoordinator` in `cmd/merges.go` (issue #2608). `prism checkin` carries the same pairing with a tier predicate rather than a role check — see Checkin scope below. No bash deny list is involved.
- A worker's terminal `finished` / `error` transition delivers a body-bearing notification to the same-repo coordinator with text `Agent <name> has finished its current task` or `Agent <name> has errored its current task`, using delivery mode `followUp`. This notification is suppressed while the worker is in `escalated` or `reviewing`.
- An **investigator** is a session created by `prism investigate`, named `<invoker>~investigate-<slug>`, spawned with `WorktreeReadOnly: true` on the 2-window `LayoutAgentOnly` layout. **`WorktreeReadOnly` is not an enforced guarantee.** No isolator reads the field today: `cmd/sidecar.go` parses the flag and discards it, `bwrap.go` binds the worktree read-write and lists the feature as deferred, and no `sandbox-exec` consumer exists. An investigator worktree is writable. Do not rely on this field for any read-only property.
- An investigator does **not** emit the `has finished` notification to the coordinator; on terminal state (`finished`, `interrupted`, `error`) it delivers a body-bearing notification to its invoker carrying the last completed turn's text, prefixed with `From investigator session: <name>` and suffixed with `Reply with: prism prompt <name> --prompt '...'`. The notification is dropped silently if the invoker has ended.
- An investigator does not self-terminate; the invoker is responsible for cleaning it up with `prism cleanup --yes --session <inv-session>`. `prism cleanup` of the invoker does **not** transitively clean up its investigator children.
- A **review agent** is a session named `<parent>~review-<round>-<role>`, created by the worker's `prism review` invocation, spawned with `WorktreeReadOnly: true` (set but unread — see the investigator entry above; the worktree is writable) on the 2-window `LayoutAgentOnly` layout, and does not run the `tmux-session-start` root-agent seeding hook.
- A review agent's terminal-state transition does **not** notify the parent worker's coordinator and does **not** notify the parent worker directly — the parent worker's `pollAgents` DB loop and the review monitor pick up the result.
- A review agent's **startup failure** (container readiness timeout, sidecar startup error, or inactivity timeout with zero inbound frames) and **mid-run stall** (inactivity timeout after one or more inbound frames, #2239) are the only cases where a review-agent session notifies its parent worker. The body is `review agent <name> failed to start: <err>` for the never-started class (the watchdog variant carries the `(no frames received)` label), or `review agent <name> stalled mid-run after <elapsed> (<n> frame(s) received, last at <t>): inactivity timeout: no inbound frame for <window>` for the stall class.
- A review agent is cleaned up by `prism cleanup` of its parent worker session — its child container, port, DB row, and tmux session are removed as part of the parent's cleanup.
- A path that **both sets state `error` and closes** a review-agent `agent_status` row writes a `session_reaped` event to `agent_events`, carrying exactly one `cause` from the set `readiness_gate`, `spawn_failure`, `monitor_timeout`, `parent_cleanup`, `cleanup_command` (issue #2613). The automatic release adds a sixth cause, `auto_release` (issue #2649); it is unlike the other five in that it closes a row that had already stopped, rather than one that was still running. The covered paths are `gateReviewAgents`, the review spawn-failure cleanup, `cleanupHalfAliveSession` (`SpawnSession`'s layout and inline-readiness failures), `forceTerminateStuckMembers`, `CleanupReviewSessionsForParent`, and `applyDBLifecycleClears`. The write is best-effort — a failed diagnostic never blocks the close.
- The record is guarded on `ended_at`, the same guard `db.SetEnded` carries. A path that finds the row already closed records nothing, so it cannot claim a close it did not perform. `SessionEndCauses` returns the LATEST `session_reaped` event, so an unguarded second record would overwrite a row's true cause with a false one — the `prism cleanup` cascade over a row a readiness gate closed hours earlier is the reachable case.
- **A closed review-agent row in state `error` with no `session_reaped` event is an expected shape, not a broken recorder.** The state can be set by one path and `ended_at` stamped later by another. The sidecar inactivity watchdog sets state `error` and writes `stall_error` (or `startup_error`) **without** stamping `ended_at`; the tmux session-closed hook then stamps `ended_at` and rewrites no state, leaving `error` in place. Neither writes a `session_reaped` event, and neither needs to: the watchdog's own event is the cause, and `classifyAbsentMember` reads `startup_error` and `stall_error` **ahead of** the reap cause. This sequence is the most likely account of PR #2610 rounds 3 and 4 — see `docs/diagnoses/review-agent-reap-2613.md`.
- The sidecar's harness `session.deleted` handler (`internal/sidecar/events.go`) also closes a review-agent row without a `session_reaped` event; it leaves state `deleted`, which the classifier reports from the state alone.
- Operator and maintenance paths that close a row record **no** cause and are out of scope for #2613: `closeSession` and `doCleanup` in `cmd/cleanup.go`, `cmd/restore.go` (no worktree recorded, or worktree not accessible), `cmd/switch_project.go`, `cmd/spawn.go`'s spawn-failure cleanup, and `db.MarkAllEnded` (`prism reset`). A row closed by one of these reports as `session ended mid-review` with the text `no close cause was recorded for this row`.
- The review round report names exactly one cause for a closed row. The order is causal: a recorded `startup_error` outranks a recorded `stall_error`, which outranks a recorded `session_reaped` cause, which outranks a `tmux_session_end` event. When nothing is recorded, the report states that no cause was recorded rather than listing candidate paths (issue #2613).
- A round's EXPECTED agent set is the full set `prism review` spawned, never the subset that became ready. An agent that failed its readiness gate or failed to spawn stays in the expected set, is reported as a missing verdict, and keeps the round incomplete (issue #2613, preserving the #2573 contract).
- A review agent is **also released automatically 15 minutes after its round's review-complete prompt is delivered**, without any operator command (#2649). The release kills the tmux session, kills the sidecar, drops the isolator's per-session temp state, releases `agent_status.harness_port`, stamps `agent_status.ended_at`, and purges queued bus messages — the same per-agent teardown `prism cleanup` of the parent runs. The window is `review.ReapGracePeriod` in `internal/review/reap.go`, and it is the number stated in the `prism` skill.
- The automatic release records `session_reaped` with cause `auto_release` **before it kills the tmux session**, which is earlier than "before it stamps `ended_at`" and is the ordering that actually holds. Killing a review agent's tmux session fires tmux's global `session-closed` hook (`modules/programs/prism/tmux.nix`), which runs `prism event tmux-session-end`, which calls `ReleasePort` and `SetEnded` — so the kill closes the row from underneath the release, and the `ended_at` guard on the cause write would then decline to record. A closed review-agent row therefore always names the release as its closer rather than reading as an unexplained close. The cause never reaches the review round report: the report is built at delivery time, and the release runs 15 minutes after that.
- Recording the cause first does not weaken the retry property. `RecordSessionReap` appends an `agent_events` row and never writes `ended_at`, so `ended_at` remains the last `agent_status` write the release performs and a release that dies part-way is still retried in full by the next sweep. If a later step fails and the row stays open, the next sweep records a second `auto_release` event; that is not de-duplicated, and it is harmless because `agent_events` is append-only, `SessionEndCauses` reads only the latest event, and both events carry the same cause.
- The automatic release deletes no row. The `agent_status`, `sessions`, `agent_events`, and `session_groups` rows all survive, so `prism checkin <parent>~review-<N>-<agent>`, `prism checkin <parent>~review`, `prism reviews list`, and `prism retro` all keep working on a released round. `prism sessions list` stops showing it, because that listing filters on `ended_at IS NULL`.
- The automatic release removes no worktree and deletes no branch. No git command runs on that path at all, so the descendant-worktree-inheritance defect fixed in #2638 cannot recur through it.
- The automatic release cannot reach a live agent. Its candidate query (`db.ReapableReviewAgents`) requires `session_groups.delivered_at IS NOT NULL` for the agent's group, and that column is written only after the round's review-complete prompt is accepted (#2259). While a round runs, delivered_at is NULL for its group, so no member of that round is a candidate — not even a member that already reached `finished` while its siblings work. Three further conditions narrow the set: `agent_status.ended_at IS NULL`, a state in `{finished, error, deleted}` (so `interrupted` is excluded, matching #1495), and `delivered_at` at or before `now - 15 minutes`. The session name is re-checked against `<parent>~review-<N>-<role>` in Go before any teardown runs.
- Every read that asks a question about a round that is already over uses `db.GroupResultsAll`, not `db.GroupResults`. `GroupResults` drops rows whose `ended_at` is set, which is correct on the live aggregation path — it is what makes the #1495 cleanup escape hatch work — and wrong for a historical read once the release has closed the round. Three sites take the wide read: `CompletedReviewCyclesForParent` (the 3-cycle LOOP-LIMIT count), `ComputeSpawnOutcome` (the fallback review-verdict roll-up), and `AbtestGroupSessions` (`prism stats abtest`). `prism retro` already took it (#2584).
- The wide read does not loosen any predicate. A member closed before it emitted a verdict is now visible with its real state and its empty message, and is rejected for that reason rather than for its absence. The #1495 abandoned-agent row reads as state `interrupted`, which is not verdict-producing, so its round still does not count.
- Every read about a round that is still running keeps the narrow `db.GroupResults`: the review monitor's aggregation, the recovery watcher's aggregation, the in-round poll loop, `prism review --wait` and its host-API `/groups/poll` counterpart, and the dashboard. None of them can observe a released row — a round is released 15 minutes after it is delivered, and each of these reads terminates at or before delivery.
- Two triggers run the release. The round's own monitor process waits out the 15 minutes after it delivers, then releases that round. `prism review` runs an unscoped sweep immediately before its concurrency-cap check, which releases every eligible round on the host — including rounds whose monitor did not survive its wait, and rounds belonging to other workers. The cap is global, so the sweep must be global to reclaim what refuses a spawn.

## Global tmux hooks

Both hooks below are registered with `set-hook -g` in `modules/programs/prism/tmux.nix`, gated only by `nx.programs.prism.tmux.enable` (default `true` — disabling it removes both hooks along with the rest of the prism tmux config). Neither hook is conditional on session kind or isolation mode at registration time; the `pane-died` handler applies its own per-invocation filter, noted below.

- **`session-closed`** — `set-hook -g session-closed "run-shell '<prism> event tmux-session-end --session #{session_name}'"`. Fires whenever ANY tmux session closes, project-owned or not. `prism event tmux-session-end` first re-checks `tmux.HasSession(session)` and returns immediately, writing nothing, when the session still exists (a display-popup dismissal spuriously reports its outer session as closed). When the session is genuinely gone and a matching `agent_status` row exists, it calls `ReleasePort` (frees the harness port) and `SetEnded` (stamps `agent_status.ended_at`), then — when the row carries an `instance_id` — calls `UpdateSessionEnded` on the `sessions` row, `ClearInstanceID` on `agent_status`, `PurgeBusMessages` for the session, and `WriteEvent` to record a `tmux_session_end` event. The review-agent auto-release race this hook causes is explained above, in the review-agent section ("The automatic release records `session_reaped` with cause `auto_release` before it kills the tmux session..."); that explanation is not repeated here.
- **`pane-exited`** — `set-hook -g pane-exited "run-shell '<prism> event pane-died --session #{session_name} --window #{window_name} --exit-code #{pane_dead_status}'"`. Fires whenever ANY pane in ANY window exits, but `prism event pane-died` is a no-op unless `--window` equals `agent` — exits from `term`, `edit`, or any other window are discarded before any DB read. For the `agent` window, when a matching `agent_status` row exists, it transitions the session to `interrupted`: a non-zero `--exit-code` overrides even a prior `finished` state (`UpsertStatusInterruptedOverrideFinished`), while a zero exit code only transitions a non-terminal state (`UpsertStatusIfNotTerminal`). Either transition path, when it applies an update, writes a `state_change` event to `agent_events`.

## Checkin scope

- `prism checkin` applies three permission tiers (issue #2587). The predicate is `authz.AuthorizeCheckin` in `internal/authz/checkin.go`. It takes the caller session as a parameter, so one copy serves both routes.
- **Both routes are gated** (issue #2619). A sandboxed caller meets the tiers on the host-API `/checkin` handler in `internal/sidecar/host_api.go`, which answers HTTP 403 / 404. A `host`-mode caller has no socket and takes the direct CLI route, where `authorizeDirectCheckin` in `cmd/checkin_permission.go` applies the same predicate and returns a non-zero exit. Both bindings call `authz.AuthorizeCheckin`; neither holds a rule of its own. `checkin` is the third member of the both-routes-gated set that #2597 (`investigate`) and #2608 (`merges`) belong to.
- The gate is not a security boundary and #2619 must not be read as one. A `host`-mode session runs with no sandbox and reads the same history with `sqlite3` against `prism.db`, without the verb. The grounds are audit completeness, correct behaviour for a cooperative caller, and route consistency.
- On the direct CLI route the caller session comes from `PRISM_SESSION_NAME`, else the current tmux session — the resolution `requireMergesCoordinator` uses. An unresolvable caller fails closed with an error naming `PRISM_SESSION_NAME`, so bare-shell `prism checkin` from a plain terminal outside tmux is refused; there is no carve-out. A prism.db that cannot be opened also fails closed, because every tier scopes on rows in it.
- The gate covers `runCheckinSession` and therefore every caller of it. Two callers exist: `prism checkin <session>` (the default view, `--json`, `--types`), and `prism checkin <parent>~review --verbose`, which renders each review-group member through `runCheckinSession` in `cmd/checkin_review.go`. The aggregate is therefore gated **per member** in verbose mode. A refused member prints `[error reading session ...]` on stderr and the loop continues; the command does not abort.
- Not gated, on either route: `prism checkin` with no argument (lists sessions, reads no history); `--compare`.
- **The non-verbose `prism checkin <parent>~review` aggregate is gated** (issue #2628). Its summary branch used to read `d.QueryEvents` inline and never reach `runCheckinSession`, so it was ungated on both routes. The question it answers — "may the caller read the review agents OF parent X" — is not the one `AuthorizeCheckin` answers: passing the parent straight in as `Target` hits the tier-1 self-checkin denial, refusing the exact worker-reads-its-own-review-group flow tier 1 exists to admit. `authz.AuthorizeCheckinReviewAggregate` in `internal/authz/checkin.go` answers the aggregate question instead: its tier-1 branch grants `Target == Caller` (the opposite of `AuthorizeCheckin`'s tier 1) and its tier-2/3 branch calls `req.authorizeCoordinator()` verbatim, so the coordinator logic is not reimplemented. On the direct CLI route, `authorizeDirectCheckinReviewAggregate` in `cmd/checkin_permission.go` applies it and returns a non-zero exit on refusal; on the host-API route, `GET /checkin/review-summary` in `internal/sidecar/host_api.go` applies it and answers HTTP 403. Inside a sandbox, `runCheckinReviewRoundsByGroup` (`cmd/checkin_review.go`) now proxies to that endpoint when `PRISM_HOST_API` is set, rather than falling through to a local `openDB()` that would fail. The verbose form's own group-listing call (session names and states, no conversation content) remains unconditional and ungated, matching the no-arg `prism checkin` precedent above.
- **Tier 1 — non-coordinator.** A worker, a review agent, an investigator, or any caller whose role cannot be determined reads the review agents of its OWN session, and nothing else. Membership resolves through `GroupParentForMember`, which joins `agent_status.group_id` to `session_groups.parent_session` and applies no name heuristic. Every other target answers HTTP 403: the caller's own session, another worker, a coordinator, and any session in another repo.
- A review agent from an earlier round stays in scope, because each round registers its own `session_groups` row against the same `parent_session`. A review agent whose `session_groups` row was deleted falls out of scope and answers 403, not 500 — the row, not the `<parent>~review-<round>-<role>` name, is the evidence.
- **Tier 2 — coordinator.** Every session in the caller's own repo, plus a cross-repo target that is itself a coordinator. This tier did not change in #2587.
- **Tier 3 — coordinator of a privileged repo.** Every session in every repo. The repo list is `Config.CheckinPrivilegedRepos`, read once at sidecar start from `~/.config/prism/checkin-privileged-repos.json`, which the prism NixOS module renders from `privilegedRepos`. An empty or absent list grants the privilege to nobody.
- The tier-3 check requires a DB-backed `root_agent_name` of `coordinator` for the caller. It does not accept the `<repo>@main` name heuristic that `isCoordinatorSession` ORs in, so a caller with no row, a NULL column, or a `worker` value is refused.
- The tier-3 privilege covers `/checkin` alone. `/db/query`, `/db/schema`, `/db/tables`, `/spawn`, `/merge`, and every other endpoint keep the gate they had.
- Tier 3 is consulted only where tier 2 refuses. Each access it admits writes one `audit` event against the caller's session with the target in `payload.Audit.Target` and the grant name in `payload.Audit.Grant`, on either route. `payload.Audit.Grant` is `authz.CheckinPrivilegeGrantName` on both; `payload.Audit.Tool` records which route admitted the read — `prism-host-api` or `prism-cli`. A tier-3 `prism checkin <parent>~review --verbose` writes one audit event **per member**, not one per command, on either route: the host-API route issues one `/checkin` per member, and the direct route calls `runCheckinSession` per member. Each member's history is a distinct privileged read.
- `prism audit` reads those events on both routes (issue #2618). `runAudit` in `cmd/audit.go` opens the prism DB directly when `PRISM_HOST_API` is unset. When it is set, the read proxies to `GET /audit`, because `$XDG_STATE_HOME/prism` is never bound into a sandbox. The default privileged seat is a sandboxed coordinator, so the trail must be readable by the seat that writes it.
- `/audit` is coordinator-only, and its gate is `requireCoordinator` — the same gate as `/db/query`. Audit rows are `agent_events` rows with `type = 'audit'`, and `/db/query` already reads every `agent_events` row for a coordinator, so the gate copies an existing decision. The tier-3 `checkin` privilege is not consulted on this route.
- The `type = 'audit'` filter lives inside `db.QueryAuditEvents` and is not a request parameter. `/audit` accepts `session`, `since`, `pattern`, and `limit`, and each one narrows the result further. No caller can widen or drop the type filter, so the route cannot become a general cross-session payload reader.
- **Both routes are gated** (issue #2627). A sandboxed caller meets `requireCoordinator` on the host-API `GET /audit` handler, which answers HTTP 403 for a non-coordinator. A `host`-mode caller has no socket, so `fetchAuditEventsLocal` in `cmd/audit.go` calls `requireAuditCoordinator` (`cmd/audit_permission.go`) before it reaches `QueryAuditEvents`, and that guard applies the same coordinator-only rule, keyed on the caller session resolved by `review.LookupParentSession()` (`PRISM_SESSION_NAME`, else the current tmux session). An unresolvable caller fails closed with an error naming `PRISM_SESSION_NAME`, before `openDB`'s result is ever queried. `audit` is the fourth member of the both-routes-gated set that #2597 (`investigate`), #2608 (`merges`), and #2619 (`checkin`) belong to. As with those three, this is not a security boundary: a `host`-mode session runs with no sandbox and reads the same rows with `sqlite3` against `prism.db`, without the verb. The grounds are correct behaviour for a cooperative caller and route consistency.
- The rendered `checkin-privileged-repos.json` is not bound into any sandbox. `StandardSandboxMounts` exposes only `~/.config/prism/agents` and `~/.config/prism/profiles.json` out of that directory, both read-only, and `generateProfile` names only the same two paths.

## Merge queue

- The host-API role gate refuses `prism merge <pr>` with HTTP 403 for every non-coordinator role; only coordinators may enqueue.
- `prism merges list` and `prism merges cancel` are coordinator-only on both routes: `requireCoordinator` on `/merges` and `/merges/cancel` for a sandboxed caller, and `requireMergesCoordinator` in `cmd/merges.go` for the direct CLI path a `host`-mode session takes (issue #2608). The read-only list verb is guarded as well as the cancel verb, so the role boundary of the verb does not change with the caller's isolation mode. On the direct path, an unresolvable caller session fails closed with the identity error (`cannot determine caller session`), and a resolvable non-coordinator caller fails closed with the role error; neither reaches `CancelMerge`.
- The merge-queue watcher is a goroutine started by the coordinator's sidecar at init; its lifetime equals the coordinator session's lifetime — there is no persistent daemon across coordinator sessions. It polls the queue head every 30 seconds (#2420, down from 45s) and processes one PR at a time.
- When the queue head's `mergeStateStatus` is `BEHIND`, the watcher itself calls `gh pr update-branch` (only for the PR at the head of the queue, just-in-time) and leaves the row `watching` for the next tick to observe the synced state (`internal/mergequeue/watcher.go`). The invocation-time initial-state message for a `BEHIND` PR names this and instructs the coordinator not to run `gh pr update-branch` manually, because a PR below the head is invalidated by every merge ahead of it in the queue (issue #2654).
- Enqueued merge-queue rows carry the coordinator's `instance_id`. On coordinator-session shutdown, every `watching` row for that `instance_id` is transitioned to `abandoned` with `error = 'coordinator session ended'`. `abandoned` rows do not produce a live notification and surface only via `prism merges list --abandoned`.
- A new coordinator incarnation for the same session name starts with an empty `watching` view and can re-enqueue any abandoned PR with `prism merge <pr>`.
- Each terminal outcome (`merged`, `failed`, `cancelled`, `abandoned`) delivers a single bus notification to the coordinator session; the `merged` text includes the worker's archive path when known.
- `prism merges list` without flags shows only the rows for the current coordinator's `instance_id`; prior-incarnation rows are not shown unless `--abandoned` or `--all` is passed.
- `prism merge <pr>` is idempotent on `(pr_number, instance_id)` for non-terminal states; a repeat enqueue on a `watching` row returns without inserting a duplicate.

## Notifications

- The worker → coordinator `has finished`/`has errored` notification is delivered as a `prompt_async`-shaped frame via the coordinator's host-API Unix socket at `$XDG_STATE_HOME/prism/run/<sessionDirHash>/hostapi.sock`, with delivery mode `followUp`.
- Each notification carries a `delivery_id` (UUID). The receiving sidecar dedups repeats by ID before they reach the harness pipe; a repeated delivery receives `{"replayed": true}` in the HTTP response. The dedup set is in-memory per sidecar (LRU, capacity 256) and does not survive a sidecar restart.
- A notification that arrives while the recipient's PI extension is disconnected is buffered (capacity 16, FIFO drop) and flushed on the next successful handshake; replayed entries carry `replay: true` on the prompt frame. The buffer is durable: rows are persisted to `pending_replay_deliveries` keyed by `(session_name, delivery_id)`, so a `prism prompt` delivery buffered before the recipient's sidecar exits is re-armed by `restorePendingReplayFromDB` on the next sidecar startup and flushed on the next handshake (issue #2359). ON CONFLICT DO NOTHING on the primary key preserves the `delivery_id` dedup contract across restart — a coordinator's reply is delivered exactly once even if the sender retries during the partition window. The durable buffer is bounded by the same in-memory capacity (16) on restore: overflow rows are deleted from the table so the bounded-buffer invariant holds across restart.
- The `pending_replay_deliveries` table is purged on two lifecycle boundaries: `prism cleanup` / `prism close` (via `severPiResumeLinkage`, in both hard and soft sever modes — #2371) removes every row for the cleaned session name, and `event tmux-session-start` removes every row for the session name before the sidecar starts — the load-bearing hook that prevents a respawn on the same branch name (a supported flow via #2094) from resurrecting the previous incarnation's coordinator directives. The periodic `DB.Prune` job additionally sweeps rows older than the 90-day threshold as a safety net for abandoned sessions that were never explicitly cleaned up.
- The `notifyCoordinator` path writes a `bus_messages` audit row on success and on failure, and is suppressed for the coordinator itself, for review-agent sessions, for investigator sessions, and for any session currently in `escalated`.
- `prism escalate` emits a `session.escalated` bus event (distinct from `session.finished`) and writes the same payload to the calling session's own event log as type `escalation`, with fields `source`, `target`, `prompt`, `pr_numbers`, `branch`, `head_sha`, `verdicts`, and `occurred_at`.
- `prism escalate` without `--to` requires exactly one same-repo coordinator candidate; with multiple candidates it exits non-zero with the list and **no state transition**. With `--to <unknown>` it exits non-zero with **no state transition**. With zero candidates it still transitions to `escalated` and writes `"no coordinator found, please wait for a human to come check on you"` into the worker's own log.
- A `session_shutdown` PI frame writes `StateFinished` directly (no idle debounce) and calls `notifyCoordinator` exactly once.
- The inactivity watchdog (`cfg.ActivityTimeout > 0`) transitions an otherwise-non-terminal session to `error` with `note="inactivity timeout"` after the configured window of zero inbound frames; for review-agent sessions it additionally fires the parent-worker failure notification, classified by the session's inbound-frame count (#2239): zero frames ever received → a `startup_error` event plus the `failed to start (no frames received)` notification; one or more frames → a `stall_error` event plus the `stalled mid-run after <elapsed> (<n> frame(s) received, last at <t>)` notification.

## Concurrency

- No two active sessions may share an `agent_status.harness_port` — the partial-unique index on `(harness_port) WHERE harness_port IS NOT NULL` enforces this at the DB level.
- The port allocator's OS probe binds `127.0.0.1:<port>`, which is the exact address the sidecar's harness pipe binds later, so a port held by a live listener is skipped. A port whose socket sits in `TIME_WAIT` is not held: Go's `net.Listen` sets `SO_REUSEADDR`, so `TIME_WAIT` blocks neither the probe nor the later bind. The allocation range (14000–14999) sits below the lowest default Linux ephemeral port (32768), so no kernel-chosen local port can take an allocated port between the probe and the bind (measured in `internal/db/port_recycling_2613_test.go`, issue #2613).
- No two active sessions may share a tmux session name; the in-process spawn lock serialises same-name races, and tmux rejects a second `new-session -s <name>` against a live name.
- Two sessions on the same branch are rejected by the git worktree machinery: only one worktree may be checked out for a branch at a time.
- The per-session run directory under `$XDG_STATE_HOME/prism/run/<sessionDirHash>/` (12-hex SHA-256 prefix of the session name) is unique per session; the host-API socket, the harness pipe socket, and the `agent-run.log` are not shared between sessions.
- The merge queue is serial: only one PR is in flight at a time per coordinator, regardless of how many `prism merge` invocations have been made.
- `prism review` is gated behind a one-shot `git fetch origin <base>` + strict `git merge-base --is-ancestor origin/<base> HEAD` check before any review agents are spawned, where `<base>` is the PR's `baseRefName` resolved via `gh pr view <pr> --json baseRefName` (falls back silently to `main` on any lookup failure — gh missing, network error, unauthenticated, no PR for the branch, or empty `baseRefName` — #2304). On refusal no agents spawn and the cycle counter does not increment. `prism review --rebase` performs fetch + rebase + force-push against the resolved base ref inline and on conflict aborts the rebase and exits non-zero, never leaving the worktree mid-rebase.

## Transports / isolation modes

- The valid isolation modes are `bwrap` (Linux only), `sandbox-exec` (Darwin only), and `host` (no isolation). There is no `podman` mode in the current code.
- The per-session run directory layout, the host-API socket path, the harness pipe socket path, and the `agent-run.log` path are **the same across all isolation modes** — only the access path to those files from inside the sandbox differs. For `bwrap` and `sandbox-exec`, the per-session run directory is bind-mounted (or SBPL-subpath-allowed) into the sandbox so the agent reaches the sockets at the same host path used outside.
- For `host` sessions, the agent runs in the tmux pane with full host filesystem access; no sandbox setup is performed.
- PI session storage location is the **same in every mode**: the host PI sessions root — `$PI_CODING_AGENT_DIR/sessions/` when set, else `~/.pi/agent/sessions/` — for `bwrap` via an overlay mount into the sandbox, for `sandbox-exec` via the injected `PI_CODING_AGENT_DIR` env var (the sandbox shares the host filesystem; #2210). The root is shared across any PI invocation on the same `cwd`, **not owned by prism**, and survives `prism cleanup` — only the cleaned session's own transcript is removed, **on hard cleanup only**, by the #2035 resume-linkage sever, which runs after the transcript has been archived and is skipped when the archive step fails (#2219). Soft closes preserve the transcript entirely and clear only the DB resume pointer (#2371).
- The notification-delivery contract (exactly-once on `delivery_id`, `followUp` mode, dedup capacity 256, replay buffer capacity 16) is **identical across all isolation modes**.
- `prism cleanup`'s sidecar-teardown, port-release, DB-end, archive, and bus-message-purge steps are **identical across all isolation modes**; only the isolator-specific `EnsureRemoved` step differs (and is a no-op for `host`).
- The concurrency cap is per-isolator: `bwrap` and `sandbox-exec` each enforce their own cap independently via `Isolator.Cap`; `host` is uncapped (`Isolator.Cap` returns `Limit: 0`).
- A coordinator running inside a sandbox proxies CLI calls (`prism cleanup`, `prism logs`, `prism merges`, etc.) through `PRISM_HOST_API` to the host sidecar; the host-side prism DB and merge queue remain the source of truth regardless of which side the CLI is invoked from.

### Go toolchain and caches (`sandbox-exec`)

- A `sandbox-exec` session can run `go build ./...` and `go test ./...` with no extra environment setup. The Go caches at their default Darwin paths — `~/go/pkg/mod` (GOMODCACHE) and `~/Library/Caches/go-build` (GOCACHE) — are readable and writable from inside the sandbox, and are shared with the host rather than per-session, so a second session starts with a warm cache.
- Both cache directories exist on the host before the sandbox starts: prism creates them at session-prepare time, because a sandbox grant on a path that does not exist grants nothing and the sandboxed process cannot create it.
- No path outside those two directories is reachable as a result. In particular `~/go/bin` — where `go install` places binaries that are typically on the host `PATH` — is **not** reachable from inside the sandbox.
- **Nothing can be executed from `~/go/pkg/mod`.** The module cache holds dependency source and is writable by the agent, so execution from it is denied outright. Copying a binary there and running it fails, even though writing it succeeds.
- Binaries **can** be executed from `~/Library/Caches/go-build`. This is required: on a warm build the Go toolchain can run a linked test binary directly out of the build cache.
- `GOTOOLCHAIN` is `local` inside the sandbox, and cannot be changed from within it. Nix is authoritative for the Go toolchain version. A sandboxed agent never downloads a Go toolchain from the internet.
- When a project's `go.mod` requires a newer Go than nix provides, the build **fails loudly** — `go: go.mod requires go >= <version> (running <version>)` — instead of downloading one. The fix is to bump the nix-pinned Go, not to widen the sandbox.
- These last two statements are one mechanism, not two independent settings: the toolchain auto-download writes into `~/go/pkg/mod` and then executes from it, which the module-cache execution denial blocks. Changing either without the other either breaks `go build` on a `go.mod` version bump or makes the module cache executable again.

### Go caches (`bwrap`)

- A `bwrap` session shares the same two Go caches with the host, at their Linux default paths — `~/go/pkg/mod` (GOMODCACHE, the GOPATH default, the same path as Darwin) and `~/.cache/go-build` (GOCACHE; `os.UserCacheDir()` is `~/.cache` on Linux, not `~/Library/Caches`). Both are read-write and shared with the host rather than per-session, so a second session starts with a warm cache (issue #2731).
- Both directories are created on the host before the sandbox starts. If that creation fails, the two binds are skipped and the session still starts: `go` then builds into the sandbox interior, which is the behaviour before #2731. The bind is conditional because bwrap stops at startup if a bind source does not exist (issue #2243).
- Both cache directories survive a reboot on a machine with impermanence. `~/.cache/go-build` rides the `.cache` entry in `modules/system/impermanence.nix`. `~/go/pkg/mod` has its own entry in `modules/programs/prism/prism-tui.nix`, because `~/go` is not persisted. Both entries are load-bearing for the warm-cache invariant above: the root subvolume is recreated on every boot, so a cache directory that is not persisted starts empty after each boot.
- No path above the two leaf directories is bound. `~/go/bin` and the remainder of `~/.cache` stay unreachable through this grant.
- Binaries **can** be executed from `~/go/pkg/mod` in a `bwrap` session, where a `sandbox-exec` session denies it. A bwrap bind mount cannot express per-path execution denial, so the Darwin rule has no Linux equivalent. The grant gives the agent no capability it lacks — the sandbox already permits execution from the worktree, `/tmp`, `~/.npm`, and `~/.cache/bun` — and both platforms grant WRITE to the module cache, so the host-side risk is the same on each.
- `GOTOOLCHAIN` is **not** pinned in a `bwrap` session, so Go's default of `auto` applies. A `go.mod` that requires a newer Go than nix provides downloads a toolchain into the shared module cache instead of failing loudly. This differs from `sandbox-exec`, where the pin and the module-cache execution denial are one mechanism.
- **Decision (issue #2737, 2026-08-09): keep `auto` for `bwrap`. No pin, no code change.** bwrap deliberately runs `GOTOOLCHAIN=auto` while Darwin pins `local` — this is an intentional platform divergence, not an oversight.
  - **Residual consequence.** A `go.mod` whose Go directive outgrows the nix-provided toolchain causes a toolchain to be downloaded into the now-shared, host-visible, exec-capable module cache at `~/go/pkg/mod/golang.org/toolchain@.../bin/go`, rather than into a throwaway sandbox interior.
  - **Mitigating facts**, so a future reader does not re-escalate this: the toolchain module is checksum-verified on download like any other Go module, so this is not an unauthenticated-code path; the machines are single-user; and Darwin already grants the same WRITE to its module cache — its `sandbox-exec` execution denial blocks in-sandbox execution only, not the write, and not a later host-side exec.
  - **The trade-off in the other direction — the stronger argument against `local`, and the reason `auto` was kept**: with `auto`, an agent can build with a different Go version than CI uses, and that divergence is silent. That was weighed and accepted in exchange for builds that keep working without intervention — with `local`, a `go.mod` that needs a newer toolchain fails loudly instead, which trades a silent version divergence for a build that stops working until a human bumps the nix-pinned Go.
